import { IProgramOptions, IProgramResult, IProgramService } from "../program/interface";

export interface IProgramSetOptions extends IProgramOptions{};

export interface IProgramSetResult extends IProgramResult{};

export interface IProgramSetService extends IProgramService{};